# min   hour    day     month   weekday command
*/5     *       *       *       *       /app/bin/mtupdate -db -no-enrollment -password=$POSTGRES_PASSWORD -host=localhost -port=$POSTGRES_PORT 2>&1 >> /app/cron.log
*/30    *       *       *       *       /app/bin/mtupdate -db -enrollment-only -password=$POSTGRES_PASSWORD -host=localhost 2>&1 >>/app/cron.log
